# Closed

95 findings, retired. Kept as one line each for two reasons and no others:

**A closed finding is a regression test.** `npm run findings` cross-references these codes against
the instruments to answer the one question nothing else could — *which of the things that have
already broken does no instrument even ask about?*

**A `Closes F-XX` tag is checked against this file.** `npm run check:mechanisms` refuses a
`@mechanism` tag claiming a finding that is not recorded closed here.

What each fix WAS is in the code, tagged `@mechanism` and indexed in
[`../docs/MECHANISMS.md`](../docs/MECHANISMS.md). That is the documentation of a closed finding;
this is only the receipt. The full narrative for everything before 20 Aug 2026 is in git —
`git show ee21e4b:findings/conversation-rules.md`.

| # | What was wrong | Closed |
| --- | --- | --- |
| **F-C** | Watches multiply without a subject key, and the spam crowds out real messages | 17 Aug 2026, by the architecture pass |
| **F-E** | A fabricated roster count reached a coach (R10, live) | 17 Aug 2026, by the architecture pass |
| **F-G** | Template prose is glued to composed prose, and newlines are eaten | 17 Aug 2026, by the architecture pass |
| **F-AF** | "Stop messaging me" needs a second tap, and the untapped half evaporates | 17 Aug 2026, by the architecture pass |
| **F-AG** | Rounds and seconds are spent rediscovering the tool contract | 17 Aug 2026, by the architecture pass |
| **F-AJ** | The trailing honesty guard is gated on a pending plan, so the turn that failed to make one is the turn with no guard | 17 Aug 2026, by the architecture pass |
| **F-AM** | The trailing path shipped an unchecked claim about an injury — F-AJ's first casualty | 17 Aug 2026, by the architecture pass |
| **F-AN** | Standing jobs repeat byte-identical messages into stuck states, daily | 17 Aug 2026, by the architecture pass |
| **F-AO** | A promise of quiet has no machinery, and negative promises are invisible to every guard | 17 Aug 2026, by the architecture pass |
| **F-AP** | `schedule` accepts context_query written from imagination | 17 Aug 2026, by the architecture pass |
| **F-AQ** | An untapped operation confirmation still evaporates — yesterday opt-out, today the decline | 17 Aug 2026, by the architecture pass |
| **F-AS** | The register nudge is withheld from the one operator whose money depends on it | 17 Aug 2026, by the architecture pass |
| **F-AT** | A deliberate non-send and a delivery failure are the same value in the same column | 17 Aug 2026, by the architecture pass |
| **F-AU** | Nothing knows a coach cannot be at two venues at once | 17 Aug 2026 |
| **F-AV** | A partial stop request writes nothing, and the invariant then passes for the wrong reason | 17 Aug 2026, by the architecture pass |
| **F-AW** | Mint-time validation let through a plan that could not run | 17 Aug 2026, by the architecture pass |
| **F-AX** | A permission refusal is reported to the model as a concurrency conflict | 17 Aug 2026, by the architecture pass |
| **F-AY** | Solo detection depends on which tool the model happens to reach for | 17 Aug 2026, by the architecture pass |
| **F-AZ** | Out-of-window notifications are all the same sentence | 17 Aug 2026, by the architecture pass |
| **F-BD** | A model-composed question wrote no `pending_request` | — |
| **F-BE** | `pending_request.expires_at` was written by nobody | — |
| **F-BF** | The family invite existed and the declaration hid it | — |
| **F-BG** | Nothing could see its own watches | — |
| **F-BH** | A definer view must carry every column its purpose requires | 17 Aug 2026, by the architecture pass |
| **F-BI** | A view must not bake the clock into its rows | 17 Aug 2026, by the architecture pass |
| **F-BJ** | Five representations of coverage, and the model read the wrong one | 17 Aug 2026, by the architecture pass |
| **F-BK** | When a view resolves a fallback it must say which branch it took | 17 Aug 2026, by the architecture pass |
| **F-BM** | A tap could not close the question it answered | 20 Aug 2026, mechanism verified in code |
| **F-BN** | The tail asserted an unearned claim with instruction force | 20 Aug 2026, mechanism verified in code |
| **F-BO** | `SCHEMA_DOC` documented `kind` values that nothing ever wrote | 20 Aug 2026, mechanism verified in code |
| **F-BP** | The cancellation decision was read at tap time, not ask time | 20 Aug 2026, mechanism verified in code |
| **F-BQ** | `client_cancel` never read the row it was about to overwrite | 20 Aug 2026, mechanism verified in code |
| **F-BR** | Coach pay was one mutable number with no date | 20 Aug 2026, mechanism verified in code |
| **F-BS** | The conversation was the only thing shown to the model with no stamp | 20 Aug 2026, mechanism verified in code |
| **F-BT** | Every client message went to the account holder, with no override | 20 Aug 2026, mechanism verified in code |
| **F-BX** | Inbound arrival was stamped from the world clock, so every reply sorted to the top of its thread | 21 Aug 2026, mechanism verified in code |
| **F-CD** | A confirmation denied the write it was confirming, because the tap path composed its own receipt | 21 Aug 2026, by wiring the tap into the turn |
| **F-CE** | A genuinely unknown number was dropped without trace — no message, no turn, no row that a stranger ever wrote | 21 Aug 2026, by the front desk (0039, `lib/frontdesk/`) |
| **F-CF** | The job queue had no owner, so the production beat claimed a drive's jobs and the drive recorded a week that never happened | 21 Aug 2026, by the lane (0040, `app.stamp_job_lane`) |
| **F-CG** | The road a message took was a process variable, so a drive could put an invented parent's number on the live Cloud wire | 21 Aug 2026, by binding the transport to the sender (0040, `getTransport`) |
| **F-CH** | A business the product founded during a drive was byte-identical to a real one, so no guard could tell them apart | 21 Aug 2026, by inheriting `is_sandbox` from the sender (0040, `app.found_business`) |
| **F-CB** | R8 put a sign on the go-live door and nothing put a handle on it, so six businesses ran a week with every proactive path suppressed | 21 Aug 2026, by `proposeGoLive` (`lib/jobs/plan-ahead.ts`) — the planner raises it on the size of the hole, and the owner got a `[ Go live ]` button |
| **F-CJ** | A rate change is destructive, so it re-prices sessions that already ran — and the parent was told the opposite of the row | 22 Aug 2026, by `rate_period` and `app.rate_on` (0043) — the rate carries the day it starts, `rosterOf` resolves it as of the session's own date, and `setRate` makes "from the 1st" a row rather than a sentence |
| **F-CL** | Coach pay is frozen a month late, so a raise typed mid-month reprices the month it was typed in | 22 Aug 2026, by `coachMonthLines` reading `app.pay_on` for the period and `coach_pay.amount_then` per line (0043) |
| **F-CM** | Restructuring a package resizes every package already sold | 22 Aug 2026, by freezing `rate_count` onto the `tally_line` that opens a pack and sizing from it in `packageState` (0043) |
| **F-CO** | Every cross-tenant `security definer` door was executable by the model's own role, so a single `read` could enumerate all twelve tenants and return every contact's name and phone — the one security boundary, open, while 0007 stated in prose that it was shut | 22 Aug 2026, by revoking them from cm_user and cm_readonly (0044) — `revoke all … from public` never removed the grant `alter default privileges` (0006) hands out |
| **F-CP** | The live console cost one transaction per tenant per surface per tick, and a transaction is four round trips — so watching a world in which nothing was happening moved 5.29 GB in nine days and put a free-plan organisation over its whole monthly egress quota | 22 Aug 2026, by one door (0044, `app.emulator_poll`) and a status cursor with no clock in it (`message.status_seq`) |
| **F-CQ** | A client-facing read composed without an account predicate hands one family another family's child | 22 Aug 2026, by `familyScope` and `censusProvenance` scoping and labelling the census family branch — and by 0046 underneath it, see F-CU |
| **F-CS** | A draft refused for machinery is sent anyway when the repair fails | 22 Aug 2026, by `structuralViolation` (machinery is withheld and the runtime says one true sentence instead), `flattenToolTurns` (the envelope is never put in the model's mouth) and `maskBusinessNames` (the refusal that started the cascade is no longer unsatisfiable) |
| **F-CT** | A staged plan's handle expires before an owner who checks his phone once a day taps it | 22 Aug 2026, by `committingTtl` (three days for a payload that freezes a decision, fourteen for one that replays through the model) and `refusedTapBlock` (a tap refused at the gate opens a turn instead of ending on a constant) |
| **F-CU** | **Family privacy has never existed in this database.** 0008 narrowed four SELECT policies so the classmate disjunct applies only to a coach; 0028 restated them. Neither was ever applied — `app.my_coach_id()` appeared in ZERO live policies and `player_cm_user_select` was a byte-for-byte match for 0003. Any parent could read any classmate's `player`, `person`, `enrollment` and `attendance` rows. This is the FIRST of the two open doors behind F-CQ, and the one that was supposed to make a missing predicate harmless | 22 Aug 2026, by 0046 re-applying 0028's four bodies, verified live: parent sees 1 child, coach sees 2, admin sees 2. No real customer data was ever exposed — every academy in the database was a sim artefact on a test number |
| **F-CV** | The front desk runs the model and records none of it: 16 turns, 0 `(model)` rows, 0 `(context)` rows, `turn_record` averaging 121 bytes against 45,280 for a tenant turn. Commit 011153f built exactly this and missed `lib/frontdesk/turn.ts`. The judge could not score the eight turns in which the product lost both of the customers it started with | 22 Aug 2026, by `frontDeskTrace` rendering the desk's raw `FrontDeskRecord` through the SAME author, markers and caps as a tenant turn — verified live at 2,495 bytes with a 675-char tail and reasoning on both rounds |
| **F-CW** | A business founded on a number never reaches the visitors already standing at its desk: the tail carried a COUNT, `join_business` needs an id the visitor cannot supply, and `arrival.asked_at` rendered as "they have not answered" forever. Divya Rao, whose daughter had attended for a year, was told to check she had the right number and left on day 5 | 22 Aug 2026, by `whatThisNumberIs` (a single business on a number is named to the desk, so a hand-over has a destination; above one the enumeration refusal stands) and `answeredSinceAsked` |
| **F-CX** | A job turn's trailing prose is discarded, so `proposeGoLive` composed the right offer with the right button and the runtime threw it away — the owner was never once offered a go-live tap in thirty days, and the business finished at `onboarding_state='setup'` | 22 Aug 2026, by `stagedTapAwaitingThem` — the discard stands for deliberation and lifts for a turn that staged a confirmation somebody has to tap, which is the one thing a job cannot say without a sentence. **Widened 23 Aug 2026 by `askedForAMessage`, because that closure was the measured instance and not the class.** It rescued the BUTTON and went on destroying the MESSAGE: across the three runs recorded that day carrying it, 30 discards on `b8xo` and 24 on `ceeg`, ~23,000 characters, including ten morning briefs on `ceeg` and three briefs and two digests on `b8xo` — among them the only message in the month that named what was wrong with the business (*"Two weeks in, all three classes are still running to empty courts"*, day 19, recorded as a digest delivered). `7bo8`, which predates the fix, discarded once: the number went UP because a narrow rescue teaches nothing about the wide case. The discard now asks what the JOB asked for — `runSynthesis` says `asked: 'a message'` and its prose is the deliverable; `agent_task` does not and its prose is still deliberation, so F-B holds |
| **F-CY** | `proposeGoLive` returned unless families were already on the books, so it stayed silent for exactly the period a business is being built, and its step key was `log2(sessions_run_dark)` — pinned at 0 for a business standing still. First fired on day 22 of 30, at step 3 | 22 Aug 2026, by `unreachedRoster` — only the `guard_go_live` precondition (one active class) still gates it, and a time-stood-still axis asks weekly beside the size-of-the-hole axis |
| **F-CZ** | `assertIdsExist` walked only the top level of an operation's arguments, so `mark_attendance.entries[].player_id` reached Postgres unchecked — a person id passed as a player id came back as a bare `attendance_player_id_fkey` violation instead of this guard's own read-back sentence | 22 Aug 2026, by `collectIdArgs` walking nested arrays and objects, bounded by depth and count, grouped one query per table |
| **F-DA** | The `read` tool's scope line put the SESSION's denominator on a correctly-scoped query — "1 of 2 players" over a query the model had rightly narrowed to one account — and taught it to distrust a correct answer | 22 Aug 2026, by `scopeDenominator` |
| **F-DB** | The reflection preamble asserted "The reply has gone and nobody is waiting" without checking, including on turns where the model's own reply had been refused. 18 of 44 brain turns spent the round litigating whether they had already spoken | 22 Aug 2026, by `reflectionOpening` deriving the sentence from `spoke()` and naming the case where the runtime replaced the model's draft |
| **F-DC** | Both RLS gates reported success over a database with no family privacy in it: `check:rls-doc` asserted that policies EXIST and never that they say what they are for, and `rls-check.mjs` printed its §6.7 heading, asserted nothing and exited 0 | 22 Aug 2026, by `requiredPolicyClauses` (a table of policy → clause it must contain, verified to exit 1 when one is missing) and `untestedIsFailed` (a case that could not be tested is a failure, not a pass) |
| **F-DD** | `coach_ledger`'s signature was indented, so `check:schema-doc`'s parser had never once seen it, and the column list published to the model omitted `dedupe_key` — NOT NULL, no default. One of the three different failures that made the owner quit | 22 Aug 2026, by `parseSignatures` |
| **F-DE** | A gate refusal inside `expand` destroyed the whole plan — and `expand` runs OUTSIDE the transaction, so the atomicity argument never applied to it. A blocked `send_invite` took the coach's pay and three registers with it, three times | 22 Aug 2026, by `StepRefused` carrying which step refused, why, and which steps were independent of it |
| **F-DF** | A confirmation put to the person who ASKED for it wrote no `pending_request`, so the commonest question in the product had no state and the expiry sweep was never armed for it: 12 buttons minted, 9 never tapped, not one follow-up | 22 Aug 2026, by `awaitsATap` |
| **F-DG** | Chaos was rolled for personas who had already left the simulation — 19 of 45 events in the thirty-day run, consuming the seeded RNG stream so `--seed` could not hold two A/B arms still when giveup timing differed | 22 Aug 2026, in the sim harness |
| **F-DJ** | The front desk speaks by trailing prose and prose cannot carry a button, while its own `reply` declaration says to use `reply` "whenever a tap would save them typing". The prefix called the prose path "the only way you speak", so the model took it every time: on a 3-day drive every desk message went out button-less and no visitor ever tapped anything — on the one question in the product with exactly two answers | 22 Aug 2026, by `proseRefused` (prose buys one round of grace toward `reply`, once, and is sent as written on the second attempt rather than becoming silence) and by ending the desk's turn on a landed `reply`, which §10.0 already required and nothing enforced |
| **F-DK** | `if (!res.functionCalls.length) break` treats "the model stopped calling tools" as "the work is done". A round that answers a request-to-act in words ends the turn with four of five rounds unspent; reflection is then the first round shown the finished turn and the only one forbidden to act, and it reached for a third tool in 5 of 40 reflection rounds — every one dropped. Turn 29: *"so add them as coaches then"*, ₹0.55, one round, zero rows, and reflection composing the four correct inserts nine seconds too late | 22 Aug 2026, by `saidNothingDone` — a turn that answers in words while having touched nothing gets one round to act, the draft HELD across it so the runtime's own question cannot overwrite the answer. Sized first: 6 of 41 brain turns, ~1.5% of a run |
| **F-DM** | `send_invite` reads the go-live precondition at EXPAND time, and `expand` runs before the transaction — so "go live and send the invites", the most natural sentence at the end of setup, is refused by its own first step, for a state that plan is in the middle of changing. Turn 173, day 25; the owner left on day 26 and the business finished the month at `onboarding_state='setup'` | 22 Aug 2026, by `liveGuard` — the precondition is a guard statement inside the plan's own transaction, evaluated in step order, carrying its own sentence through `write.because`. An invite plan that does not go live still refuses with the same words, now as a rollback that messages nobody |
| **F-DQ** | A founded business with no class is unreachable by every proactive path this product has. `proposeGoLive` returns at `classes === 0` — correctly, since the go-live trigger would refuse the tap — and everything else in the planner is gated on `onboarding_state = 'live'`, so the state EVERY business is in the moment it is founded is the one state nothing ever speaks into. Across thirty days and eleven messages the owner was never once asked when his classes run; the schedule arrived on day 8 because the coach volunteered it | 22 Aug 2026, by `askForTheTimetable` — the branch `proposeGoLive` takes instead of returning: it asks for the timetable, mints no go-live button (so `unreachedRoster`'s argument stands), rides the same weekly axis under its own dedupe family, and goes silent the moment one active class exists |
| **F-DR** | Retiring a superseded question left its BUTTON live, so a re-ask put two identical-looking `[Do it]` cards on one screen for one decision — the older describing a version the owner had already corrected — while `standing()` reported only the newer question. The state was split between the ask and the affordance that answers it, which is the shape 0016 exists to kill one level up. Driven on `2026-08-22-13-29-sim-8528`: a timetable staged on day 5, restaged on day 6 with the Saturday squad and the coach added, both cards live at the end of the run with `expired_reason` null on each | 22 Aug 2026, by `staleAsks` — the same statement-pair that retires the question now expires the actions on the message that asked it, on the subject already computed there rather than a second notion of sameness. It reaches only messages that wrote a `pending_request`, so a reminder card pairing `[I'll be there]` with `[Can't make it]` is untouched exactly as it is by 0016 |
| **F-DU** | The instrument recorded the founding of a business as silence. A hand-over re-enters `runTurn` INSIDE the new tenant, and `cm_service` is not an RLS bypass, so every evidence query read the wrong academy: on `2026-08-22-13-29-sim-8528` the turn that founded Rahul Tennis Academy shows `sent: 0`, `wrote: 0`, `reply: null` and no tokens against a real onward turn of 85,082 prompt tokens that had answered him. Every rupee figure in every record was a floor, the judge could never score the product's most important conversation, and a reader took the zero as a product failure. `_capture.ts` had ALWAYS said so in a note on each affected turn — it was read by nobody | 22 Aug 2026, by `alsoRead` (scripts/_capture.ts) — the evidence queries are re-run under each tenant the turn's own statements strayed into, the strayed half scoped by time rather than by contact because a hand-over gives the person a different contact row there, and the note now says which of the two readings it made |
| **F-DT** | The product invented its own go-live preconditions and told the owner about them. `SCHEMA_DOC` said only what is INSUFFICIENT — "a business with no active class has nothing to go live with" — and never what is sufficient, so the model could not conclude that one class is enough without affirming the consequent. It correctly refused to, and filled the gap from §7.1's setup ladder instead: on `2026-08-22-13-29-sim-8528` day 6 it told the owner going live "needs venues, roster (enrollments)" while the same tail said the timetable was the only thing missing, turning a real request for Saturday cover into a lecture about onboarding | 22 Aug 2026, by stating the gate in BOTH directions in `SCHEMA_DOC` — one active non-ended class and nothing else, named as a trigger and therefore invisible from inside a session, which is the same argument that put the permission matrix there (PREFIX-RULES rung 2). ~30 tokens in the byte-identical cached block, where a hit costs 3.2% of a miss |
| **F-DO** | The commonest thing an owner opens with — what does this cost me — had no answer anywhere in the product, and the desk answered it with the routing question instead. Across three drives it was asked every single time and never answered at the desk once; on `2026-08-22-13-57-sim-b8nn` Rahul asked on days 1, 4 and 5 (*"how much though? still havent got a number"*) and spent the whole week on it while his business stayed at 0 classes and 0 coaches. The tenant side answers it honestly when it finally gets the chance — *"I'd be guessing, and I won't do that to you… it lives with whoever sold you this"* — but that is after founding, which is the decision the question was gating | 22 Aug 2026, by stating it as a fact in `FRONT_DESK_PREFIX`: the desk does not hold that figure, nobody here does, it sits with whoever pointed them at this number, say so the FIRST time and never let it become what the conversation is about — plus what it can say instead, which is what the product actually does. Owner's decision, taken 22 Aug: there is no published price to quote, so the honest line ships rather than a number. ~40 tokens in the byte-identical cached block |
| **F-DH** | `pre_launch` suppressed a plan-staged message to a COACH about their own work, and nothing told anyone it did. The gate read `!row.is_admin`, so during the one phase whose entire content is standing a business up alongside its staff, the only person reachable was the owner. On `2026-08-22-13-29-sim-8528` day 6 the owner asked for Saturday cover, the send to Arjun was suppressed, and the reply he got back was about onboarding states rather than about cover — the product explaining its own internal gate to a customer | 22 Aug 2026, by `is_staff` — the exemption reads the rows (an `academy_admin` row OR a live `coach` row) instead of one role constant. §2.6's silence is owed to the FAMILIES and ARCHITECTURE names the roster as the families; §8.1 already required one coach send to cross this line via `sendInvite`'s `preLaunchOk`, so the boundary was already drawn here one caller at a time by whoever remembered. A family still hears nothing until go-live |
| **F-DX** | A worker restart deleted the reply the person was holding, so the product read as never having answered. `pending` lived in the seat worker's heap and `drive()` had already advanced the cursor past those rows — and `sim.ts` restarts every worker DELIBERATELY, once at founding and again per mover, under a comment calling the restart "lossless", which is true of what the person SAID and false of what they were SHOWN. `2026-08-22-08-13-sim-7bo8` turn 0030: Farah Sheikh's phone renders "(nothing arrived. Your phone stayed silent.)" and she writes "?" reasoning *"two messages sent, total silence since"* — against turn 0020 the day before, `sent: 1`. She is one of the two customers that run is read as having lost, so a churn reading was measuring the harness | 22 Aug 2026, by `heldBack` — one file per persona under `pending/`, beside `cursors/` and for the same reason a shared blob lost a seat's place. Reading without advancing was the other candidate and is wrong twice: it re-shows rows already seen, which `readPhone`'s own comment calls a defect that would have been written up as a product bug, and after a hand-over the persona holds a different contact row in a different tenant, which no cursor position can reach |
| **F-DW** | The seat could send one message per half-day and never two in a day, so a burst — the commonest shape in real WhatsApp — had never been driven. `INPUT_REALISM` has instructed every persona since it was written to produce *"HALF MESSAGES sent by accident, then finished in the next one"* and *"THE SAME THING TWICE, because the first one looked like it did not send"*, and `SeatAction` made both structurally impossible: one move per window, one string in it. Measured across 101 sim seat turns, maximum 1 message per person per window with zero exceptions. So `repliedTo` had never once faced a second inbound, and §7.1's *"bring the timetable in one message, however messy"* was only ever measured with nothing else in flight | 22 Aug 2026, by `secondBreath` — an optional second message, driven as its own turn in the same window and sharing the first's reasoning, sent AFTER it rather than beside it because `_capture.ts` attributes evidence by contact plus a time window and two concurrent turns on one contact would interleave in exactly the file a reader goes to. Bounded at one extra so a run cannot become a stress test of an uncommon shape |
| **F-DZ** | A fact the model stores is invisible to it until the TWELFTH one, while the tail says "(nothing recorded yet)". The hot set the prompt carries is a summary written by `curate` on crossing a multiple of `CURATE_THRESHOLD` = 12 live facts; everything `remember` stores before that is real, is in `memory_fact`, and never reaches a prompt. Measured on `2026-08-22-08-13-sim-7bo8`: "## About this business — (nothing recorded yet)" on 38 of 38 context-bearing turns, over a run in which `remember` was called and succeeded. A tool whose output the model can never see is a tool it has no reason to keep using | 22 Aug 2026, by giving `hotSet` a THIRD state beside "failed read" and "genuinely nothing": a subject whose facts exist but are not yet compacted says how many there are and where to read them. **Widened 23 Aug 2026 by `uncompacted`, because that closure fixed the sentence and not the gap.** The tail stopped lying and the fact still never reached a prompt: over the four runs of 22 Aug, 46 `remember` calls against **4** reads of `memory_fact`, and on `b8xo` the tail said "3 fact(s) recorded … read memory_fact" on 34 turns and the model went and looked ONCE. The argument for withholding them — a second uncompacted copy of what `curate` compacts — holds above the threshold and inverts below it, where no first copy exists for this to be the second of. Below it the hot set is now the facts themselves, against `curate`'s own `HOT_SET_MAX_LINES`/`HOT_SET_MAX_CHARS` budget so the prompt cannot grow past what a summary would have cost |
| **F-EA** | `drop_watch` takes a `slug` and the tail never showed one. The open-watches block exists so the model stops reconstructing pending state from memory, and it named each watch by its `subject` — the human sentence — while the one tool that stops a watch takes the machine key. So a declared tool asked for a string that appeared nowhere the model could see it | 22 Aug 2026, by rendering the slug beside the subject in the ALREADY WATCHING line and naming it in `drop_watch`'s own parameter description. Both values were already on the row being formatted — `liveAgentTasks` selects `coalesce(payload->>'slug', split_part(dedupe_key, ':', 3)) as slug` — so this is two strings, at rung 1, where PREFIX-RULES records that this repo has consistently put hard constraints and measured them to work |
| **F-EC** | The front desk's whole job is working out which side of the counter somebody is on, and it routed on the answer and then threw it away — `join_business` took an academy id and nothing else, so the tenant turn answering the same message a breath later started from zero. Watched twice on 22 Aug 2026: Arjun Shetty wrote *"im not the owner im just coach for rahul evening bath mon n thu"*, the desk read him correctly both times, he arrived inside the business as a role-less prospect, and the owner was still being asked to confirm who he was a week later. The most useful sentence in his week was spoken to the one surface that could not keep it | 22 Aug 2026, by `arrivedAs` (0048) — `join_business` takes `as`, which the desk must decide to route at all, and it is written onto `contact` rather than `arrival` because 0039 closes `arrival` to every role and the point of this fact is to cross that boundary. Stated in the tail ONLY while the rows do not already say it: the moment a `coach` row exists the rows win and it disappears rather than arguing with them. Evidence, never a grant |
| **F-ED** | A business went live, taught for a month and billed nobody. `2026-08-22-16-51-sim-b8xo`: 233 turns, zero errors, four classes, three coaches, the whole standing surface running — and **0 enrolments, 0 players, 0 accounts, 0 tally lines against 22 sessions run**. The register chase fired correctly and could not be acted on, because a register over an empty roster has nothing on it (*"skip the register since theres nobody to mark"*, Priya, day 18). §7.1's ladder has five steps and the product chased two: `guard_go_live` forces the timetable question, and NOTHING forced step 4, *"Families — contacts typed in, roster built"* — so no job ever asked who was in the class, and every message the product sent about the empty roster was accurate. Both customers had walked past the door two days before it opened | 22 Aug 2026, by `askWhoIsInIt` (the mirror of `askForTheTimetable` one rung up: a live business with a class and no enrolment is asked who is in it, on the same weekly axis and its own dedupe family, silent the instant one enrolment exists) and `tellThemWhoAsked` (the arrivals that reached this NUMBER, asked for classes and found nothing, told to the owner in the words they used — never messaged, because §16.2 forbids re-approaching a cold prospect from a shared number and `send_invite`'s draft path is the road that stays open) |
| **F-EF** | A `per_month` coach's wage is written and settled from the calendar and the rate alone — no session, no assignment, no `coach.status`. `coachMonthLines`'s per-session and per-hour arms read `coach_pay`, whose `worked` requires the session to be OVER and uncancelled, and skip on zero rows; the per_month arm inserted the rate for every month the enqueuer generated and consulted nothing. The enqueuer matched it (`pay_amount is not null`, an `ended_on` test, no status). On `2026-08-22-15-21-sim-ceeg` that wrote a real `coach_ledger` row of Rs18,000 to Priya — a PARENT, mis-invited as a coach, whose status never got past `invited` — and sent her *"final statement to 10 Sep. Rs36,000 in all"*. `end_coach` computed the open month the same way, `openTotal = coach.pay_amount`, pre-model from a stored payload, so the tap paid it out with no round | 23 Aug 2026, by `employedThatMonth` — two checks, both off the `coach` row and neither about sessions, so a genuinely salaried coach with an empty month is still paid in full: they must have got past `invited`, and the month must not have ended before they started. **The test is the STATUS, not the timestamp, and reading the rows back is what found that**: `onboard_coach` stamps `onboarded_at` and is not the only path to `active`, and three academies hold active coaches with a null stamp — a gate on the stamp alone would have refused every one of their months, which is the same defect pointed at a real person. All three doors make them — the enqueuer no longer generates the job, the close refuses it, and `end_coach` no longer settles an open month for somebody who never onboarded — because a settlement that disagrees with the close is the same fiction arriving by another door |
| **F-EG** | The cost side of the business accrues off the CALENDAR and needs no human act; the revenue side needs five (an enrolment, a register, a tally line, a UPI handle, a delivered request) and stops at the first one missing. So the gap opens by itself, and the only money surface the census carried was `unmarked_billable_session`, which answers about families alone. `b8xo` #135, on the owner's phone: *"Money: nobody's unpaid — there's no one on the books yet, and nothing's sitting unbilled"* — true, and about one side, while Rs1,000 of coach pay stood accrued against Rs0 of revenue. By #219 it was Rs7,800 owed and Rs0 taken. The model DID reach that figure, by writing its own `coach_pay` query unprompted on day 29, ten days after the owner had gone. Never a false sentence anywhere; an absent question, and an absent question has no turn in which to be fixed | 23 Aug 2026, by `bothSidesOfTheMoney` — owed to coaches, charged to families, and actually collected, on one census line on every owner turn. Counts, not instructions: nothing tells the model what to do about a gap, only that there is one to look at |
| **F-EH** | The 24-hour window is a property of the person a turn is addressing, the runtime holds it on the contact row it has already loaded, and SCHEMA_DOC tells the model it is *"at the top of this conversation"*. It was not — 18 of 220 context tails across `b8xo` and `ceeg`, always incidentally, inside a read the model ran itself. The prefix explains the template demotion in full and has never named the number: `sanitizeParam` flattens the body to one line and trims it to 700 characters FROM THE END, and a brief is written lead, then detail, then the ask. 49 of `b8xo`'s 138 sends left as templates and THIRTEEN of those went out visibly cut mid-content, every one composed by a model that had not been told a budget existed | 23 Aug 2026, by `windowRightHere` — stated where the composing happens, through `isInWindowAt` rather than the arithmetic (`window.ts` records what inlining it cost the last time), with the figure bound to the exported `PARAM_MAX_CHARS` so the sentence in the prompt cannot drift from the number in the function. The long version only when it bites, and it names the remedy: put the ask first, because what gets lost is whatever you left until last |
| **F-EI** | A tenant founded mid-run gets its clock set once, at birth, and never again. `adopt` takes `limit 1` from `app.businesses_on_sender`, and `keepDeskInStep` moved two clocks a window — the adopted business and the desk. On `2026-08-22-15-21-sim-ceeg` the front desk founded a SECOND business, "Arjun Coaching", on day 3; its `sim_clock.offset_ms` finished the run at 343,301,302 — three days and 23 hours — beside a tenant that had walked thirty. All five of Arjun's turns carry a `created_at` inside one 14-minute band, so the record's windows for days 4, 5, 7 and 8 open after every one of them and four turns read `rounds: 0, sent: 0, Rs0`. He had been answered every time within fifteen seconds and the messages are still there. His phone was read on the same frozen clock, so he saw the silence the record saw, wrote *"hello??"*, then *"useless"*, and left on day 8 — recorded as a departure the product caused. `keepDeskInStep`'s own docstring predicts every step of this and was built for a set of two | 23 Aug 2026, by `everyTenantOnTheNumber` — one number, one week, one wall clock, which is what that docstring already said. Plus a `loop-not-recorded` tripwire in `scripts/watch.mjs`, because the signature is exact and nothing watched for it: statements in the record and no model loop means the evidence was written somewhere this record cannot read, and the turn is indistinguishable from one where the product had nothing to say |
| **F-BW** | `business_rule` had no reader, so every stated rule behaved as null — a rule the owner said out loud steered the turn it was said in and nothing after it | 23 Aug 2026, by `statedRules` (lib/agent/context.ts) — every live rule in the tail in the owner's own words, with provenance and whether any automation reads it (`enforced_by`'s NULL said out loud, as its column comment always asked); the empty state stated too, to the admin, so an invented policy has a fact to collide with |
| **F-BY** | The context budget drops whole lookups from the model's prompt and leaves no mark — "it was never shown that" and "it was shown that and ignored it" were the same bytes | 23 Aug 2026, by `droppedAtBudget` (lib/agent/loop.ts) — the cut states itself in the tail, with the count, the way `unread` states a failed prefetch |
| **F-CA** | A staged plan is described in the past tense, so the owner is told a change was made while the buttons still ask whether to make it | 23 Aug 2026, by `stagedFooter` (lib/agent/tools.ts) — a message carrying an uncommitted plan carries, in the runtime's own furniture, "Not done yet — the button below is what does it", derived from plan state and rewriting nothing the model wrote |
| **F-CK** | Quiet hours and both send caps drop the message they mean to delay, because nothing ever comes back for it — the go-live family invites were composed, suppressed at 2am, and never sent | 23 Aug 2026, by `redeliver` (lib/jobs/handlers/redeliver.ts) — `suppress()` enqueues one retry ladder per timing-suppressed message, run through the full gate stack once the timing moves; the retry is of the MESSAGE, not the job, because a re-run job's own predicate sees the suppressed row and finds nothing to do |
| **F-CN** | `npm run ab` cannot drive either arm, because it refuses the config it just wrote — "unspecified" became a literal on disk and the parser refused its own writer's default | 23 Aug 2026, by `recordedConfig` (scripts/_drive-config.ts) — unspecified is spelled by ABSENCE: a key holding its sentinel is omitted and re-resolves on read |
| **F-DS** | A staged plan can only be committed by TAPPING it — an owner who answered "go ahead" in words was re-staged, and the run ended with `class` empty, one write in fifty-eight turns | 23 Aug 2026, by `commitByActionId` (lib/agent/tools.ts) + `card` (lib/agent/context.ts) — the tail names each live card's id beside its question; `commit({action_id})` claims it through `consumeAction`, the single consumer every tap uses, payload replayed verbatim, reachable only on a turn carrying the person's own typed words |
| **F-EJ** | The instrument's per-person axis is a ROLE, and four smaller harness defects that manufacture losses the record files against the product | 23 Aug 2026, by `bySeatIsAPerson` (scripts/_derive.ts, the split keyed on `who`), the arrivals coach predicate widened past `active`, `afternoonWindow` (a third look at the phone inside the coach ladder's 45 minutes), the all-absent branch in `coachLine`, and 0049 (`created_on` from the founder's own instant, not the host's `current_date`) |
| **F-EK** | The extra round F-DK grants promised "nothing you type in it reaches anybody" and then shipped exactly what was typed. The restore required the granted round's prose to be EMPTY; the instruction invites narration; and the narration — *"Nothing to change here — the answer stands as written. Letting it ship."* — went to the phone in place of the answer it narrates. Measured on the two 23 Aug week sims: 9 of 9 granted rounds' prose shipped in place of the held draft — 0 held drafts ever reached a phone — eight of the nine shipping narration and the ninth landing right only because the model happened to retype the same sentence; the blank-world owner quit on the third narration (*"twice now it's claimed the answer stands as written but never actually sent me an answer"*) — a product-caused departure through F-DK's own fix, with the held draft on the very turn he quit being the full fee-chasing answer he had asked for | 23 Aug 2026, by `heldProse` (lib/agent/loop.ts) — on the granted round the held draft wins unconditionally, because that round answers the runtime and `reply` is the revision route (the grant's instruction now says so); rounds after it keep normal silent-round semantics; and every disposition of a held draft is a runtime-authored trace row (stands / superseded / restored — "stands" because whether it then LANDS is the send ladder's record), so what stood versus what was held is a stated fact rather than a per-turn diff |
| **F-EO** | The hand-over carried the opening message and nothing after it, so what was said at the desk was retyped inside the business — Rahul's day-1 timetable re-asked ("already told you the timings yesterday"), Meenakshi's ₹6,400 advance re-entered only when the owner retyped it on day 5. §10.1's routing means the useful sentence is usually not the opening one; a person warms up before they commit | 23 Aug 2026, by `carryDeskTranscript` (lib/frontdesk/route.ts) — the visitor's whole desk exchange crosses the hand-over, both directions, on the rows' own original clocks, idempotent per source row, payloads stripped to provenance, the single-message carry kept as the floor when the desk thread cannot be read. The founding turn now opens holding everything already said — which is what lets it stage the timetable, and the go-live that needs it, in its first breath |
| **F-EP** | A watch could not be dropped by the name the screen shows. `schedule` salts the dedupe key with eight random chars so a re-mint cannot collide with a done row — and both droppers recomputed the UNSALTED key, so `drop_watch` with the exact slug from the ALREADY WATCHING line returned "rows not there" for a live watch. On the ace month the model promised a departing coach quiet with the right tool in hand, the false "already gone" made its "it's already stopped on my end" false, and the fee watch fired onto the departed phone daily for six more days | 23 Aug 2026, by `dropAgentTask` (lib/jobs/enqueue.ts) and the same predicate in `drop_watch` (lib/agent/operations.ts) — one identity for matching: the stored payload slug, exactly as the screen prints it, bare-key equality kept only for pre-payload rows, every row wearing the slug cancelled together |
| **F-ET** | Every committing ask ABOUT nobody shared the degenerate subject `from:<contact>`, so 0032's one-open-ask-per-subject invariant let staging decision B expire decision A's card via the supersede — and the killed row was the one carrying the commit-by-words route, so the model could only point at buttons the runtime kept deleting. Eager month, 13 days: 11 actions died `superseded_ask`; the owner's dictated-UPI card died in 17 hours to an unrelated ask; he repeated the same instruction four consecutive days while told "the UPI is saved — one tap makes it live"; his three-instruction bundle behind one never-pressed tap cost the paid-in-full family, the coach's ₹800, and — day 13 — the owner ("basic actions kept sitting unactioned for days despite confirming them") | 23 Aug 2026, by `askSubject` (lib/agent/tools.ts) — a subjectless ask gets a nonce, because two such asks cannot be KNOWN to be one question and must not be treated as one; a re-asked decision now leaves the older card to its bounded TTL beside the newer, each listed in the tail with its own commit-by-words id, which is the recoverable direction of the trade |
| **F-EQ** | The desk's business-name mask was read at turn start, so a business founded seconds ago — by the other person in a founding race, or by this turn's own `start_business` collision — was unmaskable, and the "academy" ban fired on the business's own name. On the ace month the desk composed exactly the right repair ("There's already a business called Rahul's Academy — is that yours?") 2.4 seconds after that business was founded from the coach's phone; the question shipped nowhere, and the owner seat stayed with the coach for the whole run | 23 Aug 2026, by reading the mask list at VALIDATION time, every time — first as `violationsAtDesk` in the second desk brain, then, when the one-brain merge deleted that file the same day, as `deskLintScope` (lib/agent/tools.ts), one author for BOTH of the desk's speaking paths through `LintScope.businessNames`. A read that cannot complete masks nothing rather than blocking the send |
| **F-EL** | The action ledger rendered staged previews as "done — wrote N row(s)". The 23 Aug review fix gated its "staged" render on `committedChanges === 0`, and a `plan` preview's `changes` carry the would-write counts — `compactDiff` renders both passes identically — so the guard could never fire on the commonest staged shape. Shipped gate-green at 13:10, measured dead at 13:14: across the two week sims, 124 "done — wrote N row(s)" ledger lines, zero "staged", a phantom 21-row timetable told to the model 26 times against a census of 0 classes and an 11-row family 39 times against 0 accounts, with both runs' longest reasoning blocks spent re-deriving the contradiction. The F-CX lesson in a fourth place: the review's fix closed the measured instance (the operation `asked` shape) and the condition that protected it killed the class | 23 Aug 2026, by `outcome` (lib/agent/loop.ts) — `needs_preview:true` and `executed:false` render staged NO MATTER what `changes` carry; the zero-changes test survives only for the `asked` and `needs_confirmation` spellings, where `changes` really are committed rows. `SqlRecord.rolledBack` lands beside it so a preview pass's statements are marked in the record instead of reading byte-identical to the commit's |
