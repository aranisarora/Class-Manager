node_modules/
.next/
.next-build/
out/
build/
.env
.env.local
.env*.local
.secrets/
next-env.d.ts
*.tsbuildinfo
.DS_Store
npm-debug.log*
.vercel

dev-server.log

# Probe output — measurement artifacts, regenerated by scripts/probe-*.ts
.probe*/

# Agent worktrees — throwaway checkouts, never part of the tree
.claude/worktrees/
.env*

# ...but the placeholder template is the one .env file that MUST be tracked: it is
# the only committed record of which variables a deployment needs. The negation
# has to come after the pattern it exempts, or git never reaches it.
!.env.example
